<!DOCTYPE html>
<html>
  <head>
    <title>Discount Bus Quotes</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <div class='nav-bar'>
      <%if current_user && current_user.admin?%>
        <ul>
          <%= link_to "Users", admin_users_path %><%= link_to "States", admin_states_path %><%= link_to "Logout", logout_path, method: :delete%>
        </ul>
      <%elsif current_user && current_user.default?%>
        <ul>
          <%= link_to "Companies", user_companies_path(user_id: current_user.id ) %><%= link_to "Logout", logout_path, method: :delete%>
        </ul>
      <% else %>
        <ul>
          <%= link_to "Sign Up to Be a Vendor", new_user_path %><br><%= link_to "I already have an account", login_path %><br>
        </ul>
      <%end%>
    </div>

    <%= yield %>
  </body>
</html>
